<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="./css/style.css")
        title Document
    body
        -var n = 80;
        div.container
            - for (var i = 0; i < n; i++)
                .rot(style=`--k: ${(i/n).toFixed(5)}`)